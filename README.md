# Mortgage Affordability Automation

Browser automation service for UK mortgage intermediary affordability calculators.

The project can accept lender-ready JSON directly, or map raw fact-find style YAML/JSON into lender-ready JSON for all registered lenders. It then selects the correct lender adapter, fills the lender affordability calculator with Playwright, extracts the result, and returns structured JSON with optional screenshot/PDF/failure-bundle evidence. The web/API service can run lenders inline in batches locally or fan out lender work through Cloud Tasks workers with Firestore state and Cloud Storage evidence.

## Supported Lenders

The current adapter registry includes:

- Barclays
- Halifax
- HSBC
- Kensington
- Nationwide
- NatWest
- Santander
- Skipton
- Virgin Money

Each lender adapter lives under `src/adapters/<lender>/` and compiles to `dist/adapters/<lender>/`.

## Project Structure

```text
src/             TypeScript source code. Make changes here.
src/adapters/    Lender-specific Playwright automation.
src/mappers/     Raw fact-find to lender-ready mapping for supported lenders.
src/infrastructure/
                 Cloud Tasks and Cloud Storage integrations.
src/repositories/
                 In-memory and Firestore run-state repositories.
public/          Static browser interface served by the API process.
dist/            Compiled JavaScript output generated by npm run build.
samples/         Lender-ready JSON examples, raw YAML cases, and mapped sample outputs.
Mapping_xlxs/    Workbook mapping references for mapped lenders.
docs/            Field maps, deployment notes, web UI docs, and handoff documentation.
scripts/         Helper scripts for samples, mapping batches, and attached browser mode.
```

`src/` is the source of truth. Do not manually edit `dist/` for permanent changes because it is regenerated from `src/`.

## Requirements

- Node.js 20 or newer recommended
- npm
- Playwright browser dependencies

Install dependencies:

```powershell
npm install
```

If Playwright browsers are missing on a new machine, install them with:

```powershell
npx playwright install
```

## Common Commands

Type-check without writing build output:

```powershell
npm run check
```

Build TypeScript into `dist/`:

```powershell
npm run build
```

Run the default CLI sample:

```powershell
npm run dev
```

Start the compiled API/web server:

```powershell
npm run build
npm start
```

Run the current sample runner:

```powershell
npm run samples
```

Note: `scripts/run-samples.mjs` currently scans only top-level `samples/*.json`. Most regression samples live in lender subfolders and mapped-case folders, so run those directly until the runner is made recursive.

Generate lender-ready JSON from `input.yaml` for mapped lenders:

```powershell
npm run map:halifax
npm run map:barclays
npm run map:hsbc
npm run map:kensington
npm run map:natwest
npm run map:nationwide
npm run map:santander
npm run map:skipton
npm run map:virgin-money
```

Generate mapped sample folders from raw case folders:

```powershell
npm run map:halifax:cases
npm run map:barclays:cases
npm run map:hsbc:cases
npm run map:kensington:cases
npm run map:natwest:cases
npm run map:nationwide:cases
npm run map:santander:cases
npm run map:skipton:cases
npm run map:virgin-money:cases
```

List recent local failure bundles:

```powershell
npm run failures:list
```

Start the API server:

```powershell
npm run api
```

The API exposes:

```text
GET  /health
POST /runs
GET  /
GET  /api/cases
GET  /api/cases/:caseId
GET  /api/cases/:caseId/input
GET  /api/artifact?path=...
GET  /api/artifact?uri=gs://...
GET  /api/runs/:runId
POST /api/cases/:caseId/run-affordability
POST /worker/lender-task
```

## How Build Output Works

This is a TypeScript project. The calculator automation code is written in `src/` as `.ts` files.

Running:

```powershell
npm run build
```

executes:

```powershell
tsc -p tsconfig.json
```

and generates matching JavaScript files in `dist/`.

Examples:

```text
src/adapters/santander/adapter.ts    -> dist/adapters/santander/adapter.js
src/adapters/halifax/adapter.ts      -> dist/adapters/halifax/adapter.js
src/adapters/virgin-money/adapter.ts -> dist/adapters/virgin-money/adapter.js
```

So a fresh clone can recreate `dist/` with:

```powershell
npm install
npm run build
```

## Input Model

Automation inputs are lender-ready JSON documents that match `LenderReadyInput`.

The service owns:

- lender adapter selection
- input validation
- browser automation
- calculator field interaction
- result extraction
- evidence capture
- structured success/failure output

Raw fact-find style YAML/JSON mapping is implemented for all nine registered lenders:

```text
barclays
halifax
hsbc
kensington
natwest
nationwide
santander
skipton
virgin_money
```

Mapper entry points live in `src/map-<lender>.ts`, and reusable mapping logic lives under `src/mappers/<lender>/raw-to-lender-ready.ts`.

Raw sample inputs live in:

```text
samples/raw-halifax-cases
samples/raw-additional-cases
samples/test-cases
```

Mapped outputs are written to lender-specific folders such as:

```text
samples/halifax-mapped-cases
samples/barclays-mapped-cases
samples/hsbc-mapped-cases
samples/kensington-mapped-cases
samples/natwest-mapped-cases
samples/nationwide-mapped-cases
samples/santander-mapped-cases
samples/skipton-mapped-cases
samples/virgin-money-mapped-cases
```

## Sample Usage

Run one sample directly through the TypeScript CLI:

```powershell
npx tsx src/cli.ts samples/santander/01-purchase-employed-standard.json
```

Run compiled JavaScript after building:

```powershell
npm run build
node dist/cli.js samples/santander/01-purchase-employed-standard.json
```

The output shape is:

```json
{
  "lender": "santander",
  "status": "success",
  "maximumBorrowing": 250000,
  "monthlyPayment": null,
  "messages": [],
  "evidence": {
    "screenshotPath": "./artifacts/screenshots/example.png",
    "pdfPath": "./artifacts/evidence/example.pdf",
    "failureBundlePath": "./artifacts/failures/example",
    "screenshotUri": "gs://bucket/cases/.../screenshot.png",
    "pdfUri": "gs://bucket/cases/.../evidence.pdf",
    "timestamp": "2026-05-22T00:00:00.000Z"
  }
}
```

Values vary by lender calculator response and input data.

## Browser Execution Modes

The default mode is `managed`, where Playwright launches a browser owned by the service.

Some lender sites may reject fresh automation contexts but work in an existing trusted browser session. For those cases, use attached browser mode.

Start a visible browser with remote debugging:

```powershell
npm run attached:browser
```

Open:

```text
http://127.0.0.1:9222/json/version
```

Copy `webSocketDebuggerUrl`, then run:

```powershell
$env:BROWSER_EXECUTION_MODE="attached"
$env:BROWSER_WS_ENDPOINT="ws://127.0.0.1:9222/devtools/browser/..."
$env:HEADLESS="false"
npm run build
node dist/cli.js samples/halifax/test-case-1.json
```

Credentials and broker portal sessions should stay in the browser profile. Do not commit credentials or secrets to the repository.

## Configuration

Environment variables:

```text
BROWSER_EXECUTION_MODE   managed or attached
BROWSER_WS_ENDPOINT      Chrome DevTools WebSocket URL for attached mode
HEADLESS                 false to show the browser
AUTOMATION_TIMEOUT_MS    default 30000
SCREENSHOT_DIR           default ./artifacts/screenshots
FAILURE_ARTIFACT_DIR     default ./artifacts/failures
PORT                     API port, default 3000
```

Additional production/fanout variables:

```text
LENDER_WORKER_FANOUT     true to create async case runs and dispatch lender tasks
RUN_STATE_BACKEND        firestore for durable case run state
EVIDENCE_BUCKET          Cloud Storage bucket for durable evidence
GOOGLE_CLOUD_PROJECT     GCP project for Cloud Tasks/Firestore/Storage
CLOUD_TASKS_LOCATION     Cloud Tasks queue region
CLOUD_TASKS_QUEUE        Cloud Tasks queue name
WORKER_URL               Worker endpoint, usually /worker/lender-task
WORKER_SHARED_SECRET     Optional shared-secret header for worker requests
WORKER_INVOKER_SERVICE_ACCOUNT Optional OIDC service account for Cloud Tasks
LENDER_RUN_BATCH_SIZE    Inline local batch size, default 3
```

## Documentation

Useful project docs:

- `docs/PROJECT_HANDOFF.md`
- `docs/WEB_INTERFACE.md`
- `docs/RAW_MAPPING.md`
- `docs/PLAN.md`
- `docs/CLOUD_RUN_DEMO_DEPLOY.md`
- `docs/GOOGLE_CLOUD_RUN_PRODUCTION_DEPLOYMENT.md`
- `docs/WORKER_FANOUT_CLOUD_STORAGE_RUNBOOK.md`
- `docs/HALIFAX_FIELD_MAP.md`
- `docs/HALIFAX_RAW_MAPPING.md`
- `docs/BARCLAYS_FIELD_MAP.md`
- `docs/BARCLAYS_RAW_MAPPING.md`
- `docs/KENSINGTON_FIELD_MAP.md`
- `docs/SKIPTON_FIELD_MAP.md`
- `docs/VIRGIN_MONEY_FIELD_MAP.md`
- `docs/SANTANDER_COMPLETION_ANALYSIS.md`

## Development Rule

Make code changes in `src/`, then run:

```powershell
npm run check
npm run build
```

Commit source changes, sample updates, docs, and package files. `dist/` can be regenerated from source.
